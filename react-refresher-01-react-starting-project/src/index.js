import React from 'react';
import ReactDOM from 'react-dom';

import './index.css';
import App from './App';

// index.html -> div root. React only has a single div root
ReactDOM.render(<App />, document.getElementById('root'));
